[package]
name = "firmoto-macro"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
syn = {version="2.0.111", features=["derive"]}
quote =  "1.0.42"

[dev-dependencies]
macrotest = "1.2.0"

