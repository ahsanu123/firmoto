#![feature(prelude_import)]
#[macro_use]
extern crate std;
#[prelude_import]
use std::prelude::rust_2024::*;
use firmoto_macro::FromVecTToConcrete;
struct Argument {
    name: String,
    age: u8,
    value: u32,
}
fn some() {
    let name = value
        .iter()
        .find(|pr| pr.name == Some(String::from(name)))
        .ok_or(FBRequestParseErr::FieldNotFound)?;
    let name_val = address.value.as_field_u8().ok_or(FBRequestParseErr::ValueNotFound)?;
}
fn some() {
    let age = value
        .iter()
        .find(|pr| pr.name == Some(String::from(age)))
        .ok_or(FBRequestParseErr::FieldNotFound)?;
    let age_val = address.value.as_field_u8().ok_or(FBRequestParseErr::ValueNotFound)?;
}
fn some() {
    let value = value
        .iter()
        .find(|pr| pr.name == Some(String::from(value)))
        .ok_or(FBRequestParseErr::FieldNotFound)?;
    let value_val = address.value.as_field_u8().ok_or(FBRequestParseErr::ValueNotFound)?;
}
fn main() {
    {
        ::std::io::_print(format_args!("hello world\n"));
    };
}
